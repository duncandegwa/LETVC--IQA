# Dependencies
node_modules/

# Environment variables (contains secrets — never commit these)
.env
.env.local
.env.*.local

# Build output
dist/
build/

# Firebase service account key, if downloaded into the project by mistake
*firebase-adminsdk*.json
serviceAccountKey.json

# Local uploads (dev-only file storage)
backend/uploads/

# Editor/OS
.DS_Store
.vscode/
.idea/
*.log
